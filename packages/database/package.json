{
  "name": "@paillette/database",
  "version": "0.1.0",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./schema": "./src/schema.sql"
  },
  "scripts": {
    "db:generate": "wrangler d1 migrations create paillette-db",
    "db:migrate:local": "wrangler d1 migrations apply paillette-db --local",
    "db:migrate:staging": "wrangler d1 migrations apply paillette-db --env staging",
    "db:migrate:prod": "wrangler d1 migrations apply paillette-db --env production",
    "test": "echo \"No tests for schema-only package\" && exit 0",
    "test:watch": "echo \"No tests for schema-only package\" && exit 0",
    "typecheck": "tsc --noEmit",
    "lint": "eslint . --ext .ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@paillette/types": "workspace:*",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
