name = "paillette-api"
main = "src/index.ts"
compatibility_date = "2024-11-01"
compatibility_flags = ["nodejs_compat"]
routes = [{ pattern = "paillette-api.berlayar.ai", custom_domain = true }]

# Account ID - update with your Cloudflare account ID
# account_id = "your-account-id-here"

[observability]
enabled = true

# D1 Database
[[d1_databases]]
binding = "DB"
database_name = "paillette-db"
database_id = "your-d1-database-id"  # Create with: wrangler d1 create paillette-db

# R2 Storage
[[r2_buckets]]
binding = "IMAGES"
bucket_name = "paillette-assets"

# Vectorize
[[vectorize]]
binding = "VECTORIZE"
index_name = "paillette-embeddings"

# KV for caching
[[kv_namespaces]]
binding = "CACHE"
id = "your-kv-namespace-id"

# AI binding
[ai]
binding = "AI"

# Queues for async processing
[[queues.producers]]
binding = "EMBEDDING_QUEUE"
queue = "paillette-embedding-jobs"

[[queues.producers]]
binding = "TRANSLATION_QUEUE"
queue = "paillette-translation-jobs"

[[queues.consumers]]
queue = "paillette-embedding-jobs"
max_batch_size = 10
max_batch_timeout = 30

[[queues.consumers]]
queue = "paillette-translation-jobs"
max_batch_size = 5
max_batch_timeout = 60

# Environment variables (use .dev.vars for local development)
[vars]
ENVIRONMENT = "production"
API_VERSION = "v1"
LOGTO_ISSUER = "https://m2fmae.logto.app/oidc"
LOGTO_JWKS_URI = "https://m2fmae.logto.app/oidc/jwks"
LOGTO_API_RESOURCE = "https://paillette-api.berlayar.ai"
DAILY_FREE_QUERY_LIMIT = "100"
TRANSLATION_FREE_LIFETIME_LIMIT = "10"

# Staging environment (deploy from 'staging' branch)
[env.staging]
name = "paillette-api-stg"
routes = [{ pattern = "paillette-api-stg.berlayar.ai", custom_domain = true }]
vars = { ENVIRONMENT = "staging", API_VERSION = "v1", LOGTO_ISSUER = "https://m2fmae.logto.app/oidc", LOGTO_JWKS_URI = "https://m2fmae.logto.app/oidc/jwks", LOGTO_API_RESOURCE = "", DAILY_FREE_QUERY_LIMIT = "1000", TRANSLATION_FREE_LIFETIME_LIMIT = "10", EMBEDDING_INDEX_VERSION = "v1", SEARCH_FUSION_MODE = "legacy" }

# Staging resources (separate from production)
[[env.staging.d1_databases]]
binding = "DB"
database_name = "paillette-db-stg"
database_id = "ebe8ae51-30bb-40e4-9532-5b1c9bd748d3"

[[env.staging.r2_buckets]]
binding = "IMAGES"
bucket_name = "paillette-assets-stg"
preview_bucket_name = "paillette-assets-stg"

[[env.staging.vectorize]]
binding = "VECTORIZE"
index_name = "paillette-embeddings-stg"

[[env.staging.vectorize]]
binding = "CAPTION_VECTORIZE"
index_name = "paillette-caption-embeddings-stg"

[[env.staging.vectorize]]
binding = "VECTORIZE_V2"
index_name = "paillette-embeddings-v2-stg"

[[env.staging.vectorize]]
binding = "CAPTION_VECTORIZE_V2"
index_name = "paillette-caption-embeddings-v2-stg"

[[env.staging.kv_namespaces]]
binding = "CACHE"
id = "db13cd8536b34765a7a7be64401cf0b7"
preview_id = "db13cd8536b34765a7a7be64401cf0b7"

[env.staging.ai]
binding = "AI"

[[env.staging.queues.producers]]
binding = "EMBEDDING_QUEUE"
queue = "paillette-embedding-jobs-stg"

# Production environment (deploy from 'master' branch)
[env.production]
name = "paillette-api"
vars = { ENVIRONMENT = "production", API_VERSION = "v1", LOGTO_ISSUER = "https://m2fmae.logto.app/oidc", LOGTO_JWKS_URI = "https://m2fmae.logto.app/oidc/jwks", LOGTO_API_RESOURCE = "https://paillette-api.berlayar.ai", DAILY_FREE_QUERY_LIMIT = "100", TRANSLATION_FREE_LIFETIME_LIMIT = "10" }

# Production uses the main bindings defined above
